import type { ReactNode } from "react";

export const metadata = {
  title: "Architecture",
  description:
    "System architecture, receipt loop, and ML pipeline diagrams for the Lumen customer intelligence platform.",
};

export default function ArchitectureLayout({ children }: { children: ReactNode }) {
  return children;
}
