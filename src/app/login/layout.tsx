import type { ReactNode } from "react";

export const metadata = {
  title: "Sign In",
  description:
    "Sign in to Lumen to see your customer intelligence dashboard and manage win-back campaigns.",
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return children;
}
